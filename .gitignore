node_modules/
dist/
.env
*.log

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/crash.log

# Docker
*.tar
k8s/dockerhub-secret.yaml
k8s/ghcr-secret.yaml

# OS
.DS_Store
Thumbs.db

# War Room instructor-only materials (answer key / threat model) — kept on the
# instructor's disk for the debrief, never committed or pushed.
docs/warroom/instructor/
